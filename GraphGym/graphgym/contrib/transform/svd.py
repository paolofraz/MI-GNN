import torch
from graphgym.config import cfg

def svd_reservoir(graph):
	'''set node feature according SVD Feature Reduction'''
	pass #graph.node_feature = graph
	# WIP: DeepSnap saves attributes only with "node/edge/graph" in their name...